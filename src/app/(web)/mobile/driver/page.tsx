"use client";

import MobileDriverDashboardWrapper from "@/components/driver/mobile-dashboard-wrapper";

/**
 * Dashboard Mobile del Conductor
 *
 * Ruta: /driver/(mobile)
 *
 * Características:
 * - Vista optimizada para móvil
 * - Bottom sheet para solicitudes
 * - Mapa a pantalla completa
 * - Controles táctiles
 * - Navegación inferior (del layout)
 */
export default function MobileDriverDashboard() {
  return <MobileDriverDashboardWrapper />;
}
